<% layout('layout') -%>

<div class="row">
  <% for(var i=0; i<results.length; i++) {%>
  <div class="col-lg-4">
    <div class="thumbnail">
      <img src="<%= results[i].url%>" 
		   witdh="<%= results[i].tbWidth %>" 
		   height="<%= results[i].tbHeight %>" />
      <div class="caption">
      	<h3><%= results[i].visibleUrl %></h3>
        <p><%= results[i].titleNoFormatting %></p>
      </div>
    </div>
  </div>
  <% } %>
</div>